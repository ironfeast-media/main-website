// TypeScript source for optional dynamic features.
// Compile with: `tsc --outDir public --esModuleInterop` to generate `public/main.js` if desired.
export function init() {
  console.log('Ironfeast site TS initialized')
}
